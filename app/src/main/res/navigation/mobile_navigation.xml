<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/nav_songs">

    <fragment
        android:id="@+id/nav_songs"
        android:name="com.mido.hameedplayer.ui.songs.SongsFragment"
        android:label="@string/menu_songs"
        tools:layout="@layout/fragment_songs" />
    <fragment
        android:id="@+id/nav_playlist"
        android:name="com.mido.hameedplayer.ui.Playlists.PlaylistFragment"
        android:label="@string/menu_playlist"
        tools:layout="@layout/fragment_playlists">
    </fragment>
    <fragment
        android:id="@+id/nav_recents"
        android:name="com.mido.hameedplayer.ui.recents.RecentsFragment"
        android:label="@string/menu_recents"
        tools:layout="@layout/fragment_recents" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="com.mido.hameedplayer.ui.settings.SettingsFragment"
        android:label="@string/menu_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>